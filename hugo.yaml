# Configuration
baseURL: "http://localhost:1313"
title: "Snap CD Documentation"
theme: "snapcd"

enableRobotsTXT: true
enableGitInfo: true
# enableEmoji: false
hasCJKLanguage: false

# Tailwind purges every utility class it cannot prove is used, and it proves that
# by scanning hugo_stats.json — a manifest of the classes Hugo actually emitted.
# Set this explicitly: without it, Hugo only keeps the manifest up to date if the
# file happens to already exist, so a fresh clone (or a stray delete) silently
# leaves Tailwind purging against nothing.
build:
  writeStats: true

outputs:
  home: [HTML]
  page: [HTML]
  section: [HTML, RSS]

defaultContentLanguage: en
languages:
  en:
    languageName: English
    weight: 1
    title: "Snap CD Documentation"

module:
  hugoVersion:
    extended: true
    min: "0.112.0"

  workspace: hugo.work
  imports:
    - path: .

markup:
  goldmark:
    renderer:
      unsafe: true
  highlight:
    noClasses: false

enableInlineShortcodes: true

menu:
  main:
    - identifier: get
      name: Get
      url: "https://snapcd.io"
      weight: 2
    - identifier: sample
      name: Sample
      # External link, so `url` rather than `pageRef` (which resolves against the
      # site's own content). The previous value was neither: a pageRef holding an
      # absolute URL, and a malformed one at that.
      url: "https://github.com/snapcd-samples/sample-deployment"
      weight: 3
    - name: Search
      weight: 6
      params:
        type: search
    - name: GitHub
      weight: 7
      url: "https://github.com/schrieksoft/snapcd"
      params:
        icon: github

  # sidebar:
  #   - identifier: more
  #     name: More
  #     params:
  #       type: separator
  #     weight: 1
  #   - identifier: about
  #     name: "About"
  #     pageRef: "/about"
  #     weight: 2
  #   - identifier: hugoDocs
  #     name: "Hugo Docs ↗"
  #     url: "https://gohugo.io/documentation/"
  #     weight: 3

params:
  description: Snap CD technical documentation.

  navbar:
    displayTitle: true
    displayLogo: true
    logo:
      path: images/logo.svg
      dark: images/logo-dark.svg
      # width: 40
      # height: 20
      # link: /
    width: wide

  page:
    # full (100%), wide (90rem), normal (1280px)
    width: full

  theme:
    # light | dark | system
    default: dark
    displayToggle: true

  footer:
    enable: false
    displayCopyright: true
    displayPoweredBy: true
    width: normal

  displayUpdatedDate: true
  dateFormat: "January 2, 2006"

  # Search
  # flexsearch is enabled by default
  search:
    enable: true
    type: flexsearch

    flexsearch:
      # index page by: content | summary | heading | title
      index: content
      # full | forward | reverse | strict
      # https://github.com/nextapps-de/flexsearch/#tokenizer-prefix-search
      tokenize: forward

  editURL:
    enable: false

  blog:
    list:
      displayTags: true
      # date | lastmod | publishDate | title | weight
      sortBy: date
      sortOrder: desc # or "asc"

  highlight:
    copy:
      enable: true
      # hover | always
      display: hover

  comments:
    enable: false
    type: giscus

